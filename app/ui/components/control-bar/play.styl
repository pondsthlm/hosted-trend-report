
@require "app/ui/stylus/mixins.styl";

$control-bar-height = 30px;
$button-icon {
  width: 20px;
  height: 20px;
  padding: 5px;
  border: 0;
  background-color: transparent;
  background-size: cover;
}
$normalize-control-element {
  display: table-cell;
  height: $control-bar-height;
}


.control-bar__play-button {
  @extends $normalize-control-element;
  @extends $button-icon;
  background-image: image("ikon_play.svg");
  fill: #fff;
}

.break-overlay__play-button {
  @extends $normalize-control-element;
  @extends $button-icon;
  position: absolute;
  bottom: 3%;
  left: 3%;
}
